rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 限制每個人只能存取自己 /users/{uid} 節點底下的所有資料夾與文件
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}